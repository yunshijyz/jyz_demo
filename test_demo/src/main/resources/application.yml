spring:
  datasource:
    url: jdbc:mysql://localhost/base_project?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: jiang101
    driver-class-name: com.mysql.cj.jdbc.Driver



server:
  port: 9800

mybatis:
  mapper-locations: classpath*:mapper/**/*Mapper.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.example.demo.mapper: debug
